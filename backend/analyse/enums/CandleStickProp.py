from enum import Enum

class CandleStickProp(Enum):
    TOTAL_LENGTH = 'totalLength'
    BODY_SIZE = 'bodySize'
    BALANCE = 'balance'
    UPPER_WICK_LENGTH = 'upperWickLength'
    LOWER_WICK_LENGTH = 'lowerWickLength'